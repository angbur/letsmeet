import React from 'react';
import Navigation from '@components/Navigation/Navigation';

const HomePage = () => (
  <div style={{ marginTop: '2.563rem' }}>
    <Navigation />
  </div>
);

export default HomePage;
